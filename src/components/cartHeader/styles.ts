import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    imageStyle: {
        height: 24,
        width: 24,
    },
    row: {
        flexDirection: 'row',
    },
});

export default styles;
